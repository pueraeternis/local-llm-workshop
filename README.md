# 🚀 Локальное использование LLM: Практикум (2026)

Этот репозиторий содержит материалы практического занятия по теме **"Локальный инференс и оптимизация LLM"**. Мы проходим путь от понимания математики весов до развертывания высокопроизводительных систем на базе семейства моделей **Qwen3**.

## 🎯 Цели практики
1. Научиться рассчитывать ресурсы и выбирать форматы квантования под конкретное железо.
2. Развертывать локальный инференс через современные стеки: от гибкого **Transformers** до производительного **llama.cpp/Ollama**.
3. Сравнивать эффективность различных методов оптимизации (NF4, Unsloth, GGUF).
4. Интегрировать локальные LLM в приложения через OpenAI-compatible API.

## 💻 Системные требования
Занятие подготовлено и протестировано на следующей конфигурации:
- **GPU:** NVIDIA RTX 4070 Laptop (8 GB VRAM)
- **OS:** Linux (Ubuntu / WSL2)
- **Драйверы:** NVIDIA 570+ (поддержка CUDA 12.8)
- **Инструментарий:** `uv` (менеджер пакетов), Docker (для Ollama)

---

## 🛠 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/pueraeternis/local-llm-workshop.git
cd local-llm-workshop
```

### 2. Настройка окружения
Мы используем `uv` для мгновенной установки зависимостей:
```bash
uv venv
source .venv/bin/activate
uv sync
```

### 3. Запуск инфраструктуры (Ollama)
Для тестирования продакшен-стека поднимите контейнер с Ollama:
```bash
docker compose up -d
docker exec -it ollama ollama pull qwen3:8b
```

---

## 📂 Структура практикума

Весь код сосредоточен в Jupyter Notebook `practice.ipynb`. Программа разделена на логические блоки:

1.  **Математика памяти:** Интерактивный калькулятор VRAM. Понимаем, почему 8B модель не влезает в 8GB карту в полном весе.
2.  **Transformers & BitsAndBytes:** Запуск **Qwen3-4B** в BF16 vs NF4. Изучаем "Thinking Mode" и квантование "на лету".
3.  **Unsloth:** Оптимизация инференса через кастомные ядра Triton. Сравниваем скорость с ванильным HuggingFace.
4.  **Llama.cpp:** Работа с форматом GGUF, гибридный инференс (CPU/GPU Offloading) и замеры чистой производительности.
5.  **Ollama & API:** Интеграция локальной модели в Python-код через стандартную библиотеку `openai`.

---

## 📊 Ключевые выводы экспериментов

*   **Квантование (NF4/GGUF)** — обязательный стандарт для работы на Edge-устройствах. Позволяет запустить Qwen3-8B на видеокарте с 8GB VRAM, оставляя место под контекст.
*   **Unsloth** показывает лучшие результаты по скорости среди Python-библиотек за счет глубокой оптимизации вычислений.
*   **System Memory Fallback** — опасная ловушка: при нехватке VRAM скорость падает кратно (до 5-8 t/s) из-за переезда данных в оперативную память.
*   **Hardware Power:** Производительность LLM на ноутбуках напрямую зависит от Perf-state видеокарты и наличия внешнего питания.
*   **Thinking Mode:** Современные модели (Qwen3) требуют пересмотра метрик скорости — TTFT (Time To First Token) становится важнее, чем общий TPS.

---
*2026 | LLM Driven Development Workshop*